# Environments

Planned environments are local, development, staging, and production. Production data and credentials must never be copied into lower environments.

Environment-specific infrastructure configuration is intentionally absent until cloud provisioning is separately approved.
