# Infrastructure Skeleton

This directory documents the intended infrastructure-as-code boundary. Milestone 1 does not provision or configure any cloud resource.

Future Terraform must separate application/platform and SQL-execution security domains, use private Cloud SQL networking, define least-privilege service identities, and expose all cost-bearing changes for review before apply.
